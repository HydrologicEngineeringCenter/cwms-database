
  CREATE OR REPLACE TYPE "CWMS_20"."NESTED_TS_TYPE" AS OBJECT (
   SEQUENCE     INTEGER,
   tsid         VARCHAR2 (183),
   units        VARCHAR2 (16),
   start_time   DATE,
   end_time     DATE,
   DATA         tsv_array
);

 
  GRANT EXECUTE ON "CWMS_20"."NESTED_TS_TYPE" TO "CWMS_USER"
 