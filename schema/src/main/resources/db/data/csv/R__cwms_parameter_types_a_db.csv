!query
declare
    p_code  cwms_parameter_type.parameter_type_code%type := ?;
    p_id    cwms_parameter_type.parameter_type_id%type := ?;
    p_description    cwms_parameter_type.description%type := ?;
begin
    MERGE into cwms_20.cwms_parameter_type cpt
        USING dual
        ON (cpt.parameter_type_code = p_code)
        WHEN MATCHED then
            update set
                cpt.parameter_type_id = p_id,                
                cpt.description = p_description
        WHEN NOT MATCHED then
            insert( parameter_type_code, parameter_type_id, description)
            values( p_code,
                    p_id,
                    p_description
                )
;
end;
!endquery
!description
:code \s*(?<current>[0-9]*),(?<remains>.*) int
:id \s*("?)(?<current>.*?)\1,(?<remains>.*) string
:description \s*("?)(?<current>.*?)\1|(?<remains>.*) string
!enddescription
!data
1, "Total", "TOTAL"
2, "Max",   "MAXIMUM"
3, "Min",   "MINIMUM"
4, "Const", "CONSTANT"
5, "Ave",   "AVERAGE"
6, "Inst",  "INSTANTANEOUS"
7, "Cum",   "CUMULATIVE"
8, "Inc",   "INCREMENTAL"
9, "Median","MEDIAN"