ext.ORACLE_JDBC_VERSION='19.3.0.0'

subprojects {
    apply plugin: 'java'
    repositories {
        mavenCentral()
        maven {
            url = "https://www.hec.usace.army.mil/nexus/repository/maven-public"
        }
    }
}